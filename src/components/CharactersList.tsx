import type { ReactElement } from "react";

export const CharactersList = (): ReactElement => {
  return (
    <section id="characters-list">
      <header>
        <h1>Characters List</h1>
      </header>
    </section>
  );
};
